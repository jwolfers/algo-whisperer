# OpenAI API Key (required)
OPENAI_API_KEY=your_openai_api_key_here

# Server port (optional, defaults to 3000)
PORT=3000

# Node environment (optional, set to 'production' for production deployment)
NODE_ENV=development

# CORS allowed origins (optional, comma-separated)
# In production, set this to your domain(s) to restrict access
# Leave unset in development to allow all origins
# ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
